{
  "name": "@bajpai-labs/documentation",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs && node scripts/build-site.mjs",
    "preview": "vitepress preview docs",
    "deploy:pages": "npm run build && node scripts/pages-deploy.mjs"
  },
  "devDependencies": {
    "mermaid": "^11.6.0",
    "vitepress": "^1.6.3",
    "vitepress-plugin-mermaid": "^2.0.17",
    "wrangler": "^4.14.0"
  }
}
